---
title: Interactive Demo
description: Live demo of layers, scopes, and keyboard shortcut management.
---

import DemoApp from "../../../components/demo/DemoApp.tsx";

A unified demo showing how **layers** and **scopes** work together. Layers stack shortcuts by priority — higher layers consume keys first. Scopes filter which bindings are active based on context.

<DemoApp client:only="solid-js" />
