---
title: Layers Demo
description: Interactive demo of the keyboard shortcut layers system.
---

import CommandBarDemo from '../../../components/CommandBarDemo.tsx';

Layers let you stack keyboard shortcut contexts. Shortcuts in higher layers take priority — unmatched keys fall through to lower layers. This is how you build things like command bars, modal dialogs, or any UI that temporarily overrides global shortcuts.

<CommandBarDemo client:load />
